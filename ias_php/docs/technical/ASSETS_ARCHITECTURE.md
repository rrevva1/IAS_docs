# Архитектура Assets в проекте

## Обзор

Проект использует **модульную структуру** для организации CSS и JavaScript файлов. Все стили и скрипты размещены в директориях `web/css/` и `web/js/` с разделением по функциональным модулям.

## 🎯 Принципы организации

1. **Модульность** - каждый модуль (tasks, users, site) имеет свою папку
2. **Интуитивность** - структура папок сразу показывает назначение файлов
3. **Масштабируемость** - легко добавить новый модуль
4. **Единообразие** - все CSS в `css/`, все JS в `js/`

## 📁 Структура папок

```
web/
├── css/                           # Все CSS файлы проекта
│   ├── common/                    # Общие стили для всего сайта
│   │   └── site.css              # Базовые стили (типография, цвета, layout)
│   │
│   ├── vendors/                   # Стили сторонних библиотек
│   │   └── ag-grid-custom.css    # Кастомизация AG Grid темы
│   │
│   ├── site/                      # Модуль "Страницы сайта"
│   │   └── pages.css             # Стили для страниц (index, about, contact, login)
│   │
│   ├── tasks/                     # Модуль "Заявки" (основной)
│   │   ├── index.css             # Список заявок (таблица Kartik GridView)
│   │   ├── view.css              # Просмотр одной заявки
│   │   ├── form.css              # Форма создания/редактирования
│   │   ├── form-modal.css        # Модальное окно с формой
│   │   └── statistics.css        # Страница статистики с графиками
│   │
│   └── users/                     # Модуль "Пользователи"
│       └── index.css             # Список пользователей
│
└── js/                            # Все JavaScript файлы проекта
    ├── common/                    # Общие скрипты (пока пусто)
    │
    ├── vendors/                   # Скрипты для библиотек (пока пусто)
    │
    ├── site/                      # Модуль "Страницы сайта"
    │   └── pages.js              # JavaScript для страниц site
    │
    ├── tasks/                     # Модуль "Заявки"
    │   ├── index.js              # Скрипты для списка (фильтры, сортировка)
    │   ├── view.js               # Скрипты для просмотра заявки
    │   ├── form.js               # Скрипты для формы (валидация, загрузка файлов)
    │   ├── form-modal.js         # Скрипты для модального окна
    │   ├── ag-grid.js            # Конфигурация AG Grid для заявок
    │   └── statistics.js         # Графики Highcharts для статистики
    │
    └── users/                     # Модуль "Пользователи"
        └── index.js              # Скрипты для списка пользователей
```

## 🎨 Asset Bundle классы

### Базовые Assets

#### AppAsset
- **Файл**: `assets/AppAsset.php`
- **Назначение**: Базовый asset bundle, подключается на всех страницах
- **Подключает**:
  - `css/common/site.css` - основные стили сайта
- **Зависимости**: YiiAsset, Bootstrap5Asset

#### LayoutAsset
- **Файл**: `assets/LayoutAsset.php`
- **Назначение**: Стили и скрипты для основного layout (sidebar, header)
- **Подключает**:
  - `layouts/css/main.css` - стили layout
  - `layouts/js/main.js` - скрипты для sidebar
- **Используется**: В `views/layouts/main.php`

### Модульные Assets

#### TasksAsset
- **Файл**: `assets/TasksAsset.php`
- **Назначение**: Универсальный asset для всех страниц задач
- **Подключает**:
  - `css/tasks/view.css`
  - `css/tasks/form.css`
  - `css/tasks/index.css`
  - `js/tasks/view.js`
  - `js/tasks/form.js`
  - `js/tasks/index.js`
- **Используется**: В представлениях задач (view, create, update)

#### TasksIndexAsset
- **Файл**: `assets/TasksIndexAsset.php`
- **Назначение**: Легковесный asset только для списка задач
- **Подключает**:
  - `css/tasks/index.css`
  - `js/tasks/index.js`
- **Используется**: В `views/tasks/index.php`

#### StatisticsAsset
- **Файл**: `assets/StatisticsAsset.php`
- **Назначение**: Asset для страницы статистики
- **Подключает**:
  - `css/tasks/statistics.css`
  - `js/tasks/statistics.js` (включает Highcharts)
- **Зависимости**: LayoutAsset
- **Используется**: В `views/tasks/statistics.php`

#### SiteAsset
- **Файл**: `assets/SiteAsset.php`
- **Назначение**: Asset для общих страниц сайта
- **Подключает**:
  - `css/site/pages.css`
  - `js/site/pages.js`
- **Зависимости**: LayoutAsset
- **Используется**: В представлениях site (index, about, contact, login)

#### UsersAsset
- **Файл**: `assets/UsersAsset.php`
- **Назначение**: Asset для страниц пользователей
- **Подключает**:
  - `css/users/index.css`
  - `js/users/index.js`
- **Зависимости**: LayoutAsset
- **Используется**: В `views/users/index.php` и `views/users/view.php`

### Специализированные Assets

#### AgGridAsset
- **Файл**: `assets/AgGridAsset.php`
- **Назначение**: AG Grid для расширенной таблицы заявок
- **Подключает**:
  - `ag-grid-community/styles/ag-theme-quartz.css` - тема AG Grid
  - `css/vendors/ag-grid-custom.css` - кастомные стили
  - `ag-grid-community/dist/ag-grid-community.min.js` - библиотека AG Grid
  - `js/tasks/ag-grid.js` - конфигурация для заявок
- **Зависимости**: YiiAsset, Bootstrap5Asset, JqueryAsset
- **Используется**: В `views/tasks/index-aggrid.php`

## 📝 Использование в представлениях

### Пример 1: Простое подключение

```php
<?php
use app\assets\TasksIndexAsset;

TasksIndexAsset::register($this);

$this->title = 'Заявки';
?>

<div class="tasks-index">
    <!-- Контент страницы -->
</div>
```

### Пример 2: Множественные assets

```php
<?php
use app\assets\TasksAsset;
use app\assets\AgGridAsset;

// Подключаем базовые стили задач
TasksAsset::register($this);

// Дополнительно подключаем AG Grid
AgGridAsset::register($this);
?>
```

### Пример 3: С зависимостями

```php
<?php
use app\assets\StatisticsAsset;

// Автоматически подключит LayoutAsset как зависимость
StatisticsAsset::register($this);
?>
```

## 🔄 Добавление нового модуля

Для добавления нового модуля (например, "reports"):

### 1. Создайте структуру папок

```bash
mkdir -p web/css/reports web/js/reports
```

### 2. Создайте CSS и JS файлы

```bash
touch web/css/reports/index.css
touch web/js/reports/index.js
```

### 3. Создайте Asset класс

```php
<?php
// assets/ReportsAsset.php

namespace app\assets;

use yii\web\AssetBundle;

class ReportsAsset extends AssetBundle
{
    public $basePath = '@webroot';
    public $baseUrl = '@web';
    
    public $css = [
        'css/reports/index.css',
    ];
    
    public $js = [
        'js/reports/index.js',
    ];
    
    public $depends = [
        'app\assets\LayoutAsset',
    ];
}
```

### 4. Используйте в представлениях

```php
<?php
use app\assets\ReportsAsset;

ReportsAsset::register($this);
?>
```

## 💡 Лучшие практики

### 1. Именование файлов
- Используйте короткие понятные имена: `index.css`, `view.css`, `form.css`
- Для модальных окон добавляйте суффикс: `form-modal.css`
- Для конфигураций библиотек: `ag-grid.js`, `highcharts.js`

### 2. Организация кода

**В CSS:**
```css
/* Группируйте стили логически */

/* === Основные контейнеры === */
.tasks-index { }
.tasks-container { }

/* === Формы === */
.task-form { }
.form-section { }

/* === Компоненты === */
.task-card { }
.task-badge { }
```

**В JavaScript:**
```javascript
// Используйте немедленно вызываемые функции (IIFE)
(function() {
    'use strict';
    
    // Ваш код здесь
})();
```

### 3. Зависимости Assets

Правильно указывайте зависимости:

```php
public $depends = [
    'yii\web\YiiAsset',           // Базовый Yii JS
    'yii\bootstrap5\BootstrapAsset', // Bootstrap CSS
    'yii\web\JqueryAsset',        // jQuery (если нужен)
];
```

### 4. Производительность

- ✅ Подключайте только необходимые assets
- ✅ Используйте легковесные assets (например, TasksIndexAsset вместо TasksAsset)
- ✅ Yii2 автоматически минифицирует и кэширует assets в production
- ✅ Используйте `appendTimestamp` для сброса кэша при обновлениях

## 🚀 Оптимизация для Production

В файле `config/web.php` настройте AssetManager:

```php
'components' => [
    'assetManager' => [
        'bundles' => [
            'yii\web\JqueryAsset' => [
                'js' => [
                    YII_ENV_DEV ? 'jquery.js' : 'jquery.min.js'
                ]
            ],
        ],
        'appendTimestamp' => true, // Автоматическое версионирование
    ],
],
```

## 📊 Сравнение: До и После реорганизации

### ❌ Было (плоская структура)

```
web/css/
├── site.css
├── site-pages.css
├── tasks-index.css
├── tasks-view.css
├── tasks-form.css
├── tasks-form-modal.css
├── tasks-page.css          ← дубликат
├── statistics.css
└── ag-grid-custom.css
```

**Проблемы:**
- 9+ файлов в одной папке
- Непонятно какие файлы к какому модулю относятся
- Дубликаты (tasks-page.css)

### ✅ Стало (модульная структура)

```
web/css/
├── common/
│   └── site.css
├── vendors/
│   └── ag-grid-custom.css
├── site/
│   └── pages.css
├── tasks/
│   ├── index.css
│   ├── view.css
│   ├── form.css
│   ├── form-modal.css
│   └── statistics.css
└── users/
    └── index.css
```

**Преимущества:**
- ✅ Логическая группировка по модулям
- ✅ Легко найти нужный файл
- ✅ Нет дубликатов
- ✅ Масштабируемая структура

## 📈 Статистика

- **Всего модулей**: 4 (common, site, tasks, users)
- **Всего CSS файлов**: 9
- **Всего JS файлов**: 8
- **Asset Bundle классов**: 10
- **Удалено дубликатов**: 2 файла (page.css, page.js)

## 🔍 Поиск файлов

Быстро найти файлы можно командой:

```bash
# Все CSS файлы модуля tasks
find web/css/tasks -type f

# Все JS файлы проекта
find web/js -type f -name "*.js"

# Поиск по содержимому
grep -r "class-name" web/css/
```

## 🛠️ Обслуживание

### Очистка кэша assets

```bash
# Удаление скомпилированных assets
rm -rf web/assets/*

# Yii2 автоматически пересоздаст их при следующем обращении
```

### Проверка целостности

```bash
# Проверить, что все файлы из Asset классов существуют
php yii asset/check  # (если создать такую команду)
```

## 📚 Дополнительные ресурсы

- [Документация Yii2 Asset Bundles](https://www.yiiframework.com/doc/guide/2.0/ru/structure-assets)
- [Bootstrap 5 Documentation](https://getbootstrap.com/docs/5.0/)
- [AG Grid Documentation](https://www.ag-grid.com/documentation/)

---

**Последнее обновление**: 01.11.2025  
**Версия**: 2.0 (Модульная структура)  
**Статус**: ✅ Активная
