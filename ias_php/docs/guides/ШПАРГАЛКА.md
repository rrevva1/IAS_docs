# 🚀 БЫСТРАЯ ШПАРГАЛКА - IAS_UCH_VNII

## 🔥 САМЫЕ ВАЖНЫЕ КОМАНДЫ

### 📥 Обновление проекта
```bash
git pull origin main          # Загрузить изменения с GitHub
git restore .                 # Отменить локальные изменения
git clean -fd                 # Удалить неотслеживаемые файлы
```

### 🗄️ База данных
```bash
php yii migrate               # Применить миграции
php yii migrate/history      # Статус миграций
php yii cache/flush-all      # Очистить кэш
```

### 🛠️ Разработка
```bash
php yii serve                 # Запустить сервер разработки
composer install              # Установить зависимости
composer update               # Обновить зависимости
```

### 🔍 Диагностика
```bash
git status                   # Статус Git
php yii migrate/history      # Статус БД
tail -f runtime/logs/app.log # Просмотр логов
```

## ⚡ БЫСТРЫЕ ДЕЙСТВИЯ

| Действие | Команда |
|----------|---------|
| Обновить проект | `git pull origin main` |
| Применить миграции | `php yii migrate` |
| Запустить сервер | `php yii serve` |
| Очистить кэш | `php yii cache/flush-all` |
| Проверить статус | `git status` |

## 🎯 КЛЮЧЕВЫЕ ПАПКИ

- `controllers/` - Контроллеры приложения
- `models/` - Модели данных
- `views/` - Шаблоны представлений
- `migrations/` - Миграции БД
- `web/uploads/` - Загруженные файлы
- `runtime/logs/` - Логи приложения

## 🚨 ЭКСТРЕННЫЕ КОМАНДЫ

```bash
# Если что-то сломалось
git restore . && git clean -fd && git pull origin main

# Если проблемы с БД
php yii migrate/fresh && php yii migrate

# Если проблемы с правами
chmod -R 755 runtime/ web/uploads/
```

---
*Сохраните эту шпаргалку для быстрого доступа!*
