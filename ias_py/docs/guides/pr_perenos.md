Контекст и цели миграции
У нас есть монолитное приложение Help Desk/Asset Management на Yii2 (PHP) с PostgreSQL, которое требует миграции на Django (Python) по следующим причинам:

Критические уязвимости безопасности (MD5 для паролей, тестовые эндпоинты, утечки в логах)

Архитектурные проблемы (смешение бизнес-логики, отсутствие сервисного слоя)

Дрейф схемы БД (несоответствие между кодом, миграциями и дампом)

Требования enterprise: надежность, поддерживаемость, безопасность, наблюдаемость

Исходный стек: PHP 7.4+, Yii2 MVC, PostgreSQL 12+, AG Grid на фронтенде
Целевой стек: Python 3.11+, Django 5.x, PostgreSQL 14+, Django REST Framework (если нужно API), тот же фронтенд (AG Grid)

📋 Задачи миграции
Фаза 1: Анализ и проектирование (1-2 недели)
Проанализируй текущую структуру БД (дампа ias_uch_vnii_public_dump.sql):

Создай ER-диаграмму основных сущностей: users, roles, tasks, desk_attachments, arm, location

Выяви все связи, индексы, constraints

Отметь расхождения между дампом и миграциями Yii2

Сопоставь текущую функциональность:

text
Контроллеры Yii2 → Представления Django:
- TasksController → TaskListView, TaskCreateView, TaskUpdateView, TaskDetailView
- UsersController → UserCRUDView + убрать тестовые эндпоинты
- HelpDeskController → DashboardView, StatisticsView
- ArmController → AssetListView, AssetDetailView
Спроектируй доменную модель Django:

Пользователи и роли (интеграция с Django Auth)

Задачи Help Desk с вложениями

Управление активами (Asset Management)

Локации/отделы
Фаза 2: Настройка Django проекта (3-5 дней)
bash
# Структура проекта
helpdesk_ias/
├── config/              # Настройки Django
├── apps/
│   ├── users/          # Кастомная модель пользователя
│   ├── tasks/          # Задачи Help Desk
│   ├── assets/         # Управление активами
│   ├── core/           # Общие утилиты, мидлвары
│   └── api/            # DRF эндпоинты для AG Grid
├── static/
├── templates/
└── manage.py
Требования к настройке:

Используй django-environ для управления секретами (убери hardcoded пароли)

Настрой django-debug-toolbar для отладки

Подключи django-extensions для удобства разработки

Настрой логирование в отдельные файлы (security.log, audit.log)

Фаза 3: Миграция данных (2-3 недели)
Создай скрипты миграции данных:

python
# scripts/migrate_data.py
import pandas as pd
from django.db import transaction
from apps.users.models import CustomUser
from apps.tasks.models import Task, Attachment

def migrate_users():
    # 1. Конвертируй MD5 пароли в Django-совместимые (аргон2/бкрипт)
    # 2. Маппинг ролей: Yii2 role_id → Django groups/permissions
    # 3. Перенеси дополнительные поля: position, department, phone

def migrate_tasks():
    # 1. Перенеси задачи с учетом нормализованных вложений
    # 2. Обработай JSON поле attachments из Yii2
    # 3. Сохрани связи с файлами

def migrate_attachments():
    # 1. Перенеси метаданные из desk_attachments
    # 2. Скопируй файлы из /web/uploads/tasks/ в MEDIA_ROOT
    # 3. Создай связи task-attachment через ForeignKey (не JSON)
Обрати особое внимание на:

Трансформацию MD5 → bcrypt (с прозрачным апгрейдом при логине)

Нормализацию схемы вложений (убрать JSON, сделать связи FK)

Сохранение целостности ссылок между задачами и файлами

Фаза 4: Реализация бизнес-логики (3-4 недели)
Модели Django:

python
# apps/tasks/models.py
class Task(models.Model):
    STATUS_CHOICES = [
        ('new', 'Новая'),
        ('in_progress', 'В работе'),
        ('completed', 'Завершена'),
        ('cancelled', 'Отменена'),
    ]
    
    title = models.CharField(max_length=255)
    description = models.TextField()
    creator = models.ForeignKey(User, on_delete=models.PROTECT, related_name='created_tasks')
    executor = models.ForeignKey(User, null=True, on_delete=models.SET_NULL, related_name='assigned_tasks')
    status = models.CharField(max_length=20, choices=STATUS_CHOICES, default='new')
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    
    class Meta:
        permissions = [
            ("can_assign_task", "Can assign tasks to others"),
            ("can_change_status", "Can change task status"),
            ("view_all_tasks", "Can view all tasks (not only own)"),
        ]

class TaskAttachment(models.Model):
    task = models.ForeignKey(Task, on_delete=models.CASCADE, related_name='attachments')
    file = models.FileField(upload_to='task_attachments/%Y/%m/%d/')
    original_name = models.CharField(max_length=255)
    uploaded_by = models.ForeignKey(User, on_delete=models.SET_NULL, null=True)
    uploaded_at = models.DateTimeField(auto_now_add=True)
Сервисный слой (вместо бизнес-логики в контроллерах):

python
# apps/tasks/services.py
class TaskService:
    @transaction.atomic
    def create_task(self, user, data, files=None):
        # Валидация + создание задачи + загрузка файлов
        # Откат транзакции при ошибке (удаление файлов)
        pass
    
    def assign_executor(self, task_id, executor_id, assigned_by):
        # Проверка прав assigned_by
        # Обновление исполнителя
        # Логирование действия
        pass
Политики доступа (замени магические проверки Yii2):

python
# apps/tasks/policies.py
class TaskPolicy:
    @staticmethod
    def can_view(user, task):
        return user.is_superuser or task.creator == user or task.executor == user
    
    @staticmethod  
    def can_edit(user, task):
        return user.is_superuser or task.creator == user
    
    @staticmethod
    def can_change_status(user, task):
        return user.is_superuser or task.executor == user
Фаза 5: Веб-интерфейс и API (2-3 недели)
Django Admin кастомизация:

Задачи с inline вложениями

Фильтры по статусу, исполнителю, дате

Mass actions (изменение статуса, назначение исполнителя)

API для AG Grid (если фронт остается):

python
# apps/api/views.py
class TaskGridView(APIView):
    permission_classes = [IsAuthenticated]
    
    def get(self, request):
        # Пагинация (не как в Yii2 где отдаются все данные)
        # Фильтрация по правам (админ видит все, пользователь - свои)
        # Оптимизация запросов (select_related, prefetch_related)
        # Формат совместимый с AG Grid
Представления для файлов:

python
# apps/tasks/views.py
class TaskAttachmentDownloadView(View):
    def get(self, request, task_id, attachment_id):
        # 1. Проверить что задача существует
        # 2. Проверить права на задачу (TaskPolicy.can_view)
        # 3. Проверить что вложение принадлежит задаче
        # 4. Отдать файл с правильными headers
        # 5. Запретить inline для SVG (XSS защита)
Фаза 6: Безопасность и аудит (1 неделя)
Исправь критичные уязвимости:

Пароли: только django.contrib.auth.hashers (Argon2 по умолчанию)

Сессии: SESSION_COOKIE_SECURE = True, SESSION_COOKIE_HTTPONLY = True

CSRF: включить везде

XSS: экранирование в шаблонах, Content-Security-Policy

Добавь аудит:

python
# apps/core/middleware.py
class AuditMiddleware:
    def process_view(self, request, view_func, view_args, view_kwargs):
        if request.user.is_authenticated:
            AuditLog.objects.create(
                user=request.user,
                action=view_func.__name__,
                path=request.path,
                method=request.method,
                ip=request.META.get('REMOTE_ADDR'),
                user_agent=request.META.get('HTTP_USER_AGENT')
            )
Настрой RBAC:

Группы: "Администраторы", "Исполнители", "Пользователи"

Permissions: по модели Task (см. Meta.permissions выше)

Проверка прав в каждом представлении

Фаза 7: Тестирование и деплой (1-2 недели)
Напиши тесты:

python
# apps/tasks/tests/test_services.py
class TaskServiceTestCase(TestCase):
    def test_create_task_with_attachments(self):
        # Проверка транзакционности
        # Проверка что файлы удаляются при откате
        pass
    
    def test_permission_enforcement(self):
        # Пользователь не может менять чужие задачи
        # Админ может назначать исполнителей
        pass
Миграция на проде:

Backup БД и файлов

Поэтапный деплой (read-only режим на время миграции)

Мониторинг ошибок (Sentry)

Откатная стратегия

🎯 Ключевые требования к реализации
Совместимость с текущим фронтендом (AG Grid) - API должен отдавать данные в том же формате

Безопасность превыше всего - никаких MD5, тестовых эндпоинтов, утечек в логах

Целостность данных - транзакции для операций с файлами, ForeignKey вместо JSON

Производительность - пагинация, оптимизация запросов, индексы

Наблюдаемость - логирование действий, метрики, мониторинг

⚠️ Особые указания по миграции
Файлы вложений: сохрани оригинальную структуру /web/uploads/tasks/ или перенеси в Django MEDIA_ROOT

Пароли MD5: реализуй миграцию с прозрачным апгрейдом - при первом логине проверяй MD5, если совпадает - хешируй в bcrypt и обновляй

Роли пользователей: маппинг id_role из дампа → Django Groups с соответствующими permissions

AG Grid: воспроизведи тот же JSON формат ответа, чтобы не править фронтенд

📊 Ожидаемый результат
Безопасное: OWASP Top 10 покрыт, пароли хешированы правильно, нет тестовых эндпоинтов

Поддерживаемое: чистый сервисный слой, разделение ответственности, документация

Производительное: пагинация, индексы, оптимизированные запросы

Масштабируемое: модульная структура, возможность выделения микросервисов в будущем